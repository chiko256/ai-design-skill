---
name: web-design-pro
description: MCP認証付きのWeb制作セット。webデザイン、AIデザイン、TOP・LPの全体制作、Figma納品、下層ページ制作、途中再開を依頼された時に使う。単一の画像編集や一般的なWeb実装には使わない。
---

# Web制作セット

契約者用MCPサーバーから、その時点で必要な工程手順だけを取得してWebデザイン制作を進める。

## 自動更新

制作を始める前に、このSkill内の`scripts/update-client.sh`を実行する。

- `current`: そのまま進む。
- `updated`: 更新後の`SKILL.md`を全文読み直してから進む。
- `skipped`または`failed`: 利用者のファイルを上書きしない。インストール済み版で契約確認へ進み、サーバーから更新要求が返った場合だけ理由を伝えて停止する。

更新のためにローカル変更の削除、強制reset、別リポジトリへの接続変更を行わない。

## 起動

1. MCPの`web_design_contract`を呼び、契約とバージョンが有効か確認する。
2. 依頼を次のどちらかへ分類する。
   - TOP・LP・Webデザイン全体・Figma納品: `top`
   - 下層ページ制作: `lower`
3. `web_design_start`へ分類結果だけを渡す。ユーザー依頼本文は送らない。返された`sessionId`をこの依頼中だけ保持する。
4. 入口手順に従い、子工程へ入る直前に`web_design_open_step`を呼ぶ。

## 補助資料と実行ファイル

- 工程手順が読むよう指定した補助資料だけを`web_design_read_support`で取得する。一括取得しない。
- 参考画像が必要な場合だけ`web_design_read_image`で開く。
- 手順中の`<THIS_SKILL_DIR>/scripts`は、このスキルの`runtime/<skill-id>/scripts`へ読み替える。
- 手順中の`../../_shared/`は、このスキルの`runtime/_shared/`へ読み替える。
- 配布された実行スクリプトは利用者の案件ファイルに対してローカルで実行する。秘密手順本文を案件フォルダへ保存しない。

## 進行と停止

- 各工程の停止条件、ユーザー選択待ち、Figma同時書き込み禁止を守る。
- 工程の結果は`web_design_checkpoint`へ短く記録する。原稿本文、画像、顧客情報、秘密情報は送らない。
- 指定範囲の完了条件が揃った時だけ`web_design_finish`を呼ぶ。
- MCPが認証拒否、契約停止、更新要求を返した場合は回避せず、理由を利用者へ伝えて停止する。

## 外部機能

工程に応じて`imagegen`、Figma MCP、`figma:figma-use`、Playwright、Python Pillow、複数担当の起動・待機を使う。利用できない必須機能がある場合は、代替したふりをせず停止理由を示す。

## 秘密情報

MCPから受け取った詳細手順を、一覧化、転載、配布、ログへの全文保存に使わない。成果物とユーザーへの報告には、判断結果、作成物、停止理由、次工程だけを残す。
