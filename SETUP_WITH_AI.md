# AIデザインSkill：AI向け初回設定手順

## 目的

このリポジトリを受け取ったAIは、利用者の端末でAIデザインSkillとFigmaを利用できる状態まで設定する。Webデザイン制作はまだ開始しない。

## 実行ルール

- 説明だけで終わらず、利用できるツールと許可範囲で設定を実行する。
- 完了済みの設定を確認し、不足部分だけを実施する。
- 既存のCodex設定を削除しない。
- 契約者専用トークンの値を表示・記録・チャットへ転記しない。
- ログイン、トークン入力、Codex再起動など本人操作が必要な場所だけ、短く案内して待つ。
- 実際に確認できていない項目を完了扱いにしない。

## 1. リポジトリを配置する

このリポジトリが、Codexのユーザー用Skillフォルダ内にあるか確認する。

標準の配置先：

```text
~/.codex/skills/web-design-pro
```

まだ配置されていない場合は、このGitHubリポジトリを上記へcloneする。すでにZIP版などの同名フォルダがある場合は、内容と利用者の変更を確認し、復元できるように保全してからGitHub版へ移行する。

配置後、次のファイルが存在することを確認する。

```text
~/.codex/skills/web-design-pro/SKILL.md
~/.codex/skills/web-design-pro/.git
```

`scripts/update-client.sh`を一度実行し、自動更新の確認結果が`current`または`updated`になることを確認する。許可画面が表示された場合は、GitHubからの更新確認に必要な操作だと利用者へ案内する。

## 2. MCP接続設定を追加する

Codexが使用している `config.toml` を確認する。変更前に復元できる状態にし、既存設定を保ったまま `codex-config.example.toml` のAIデザインSkill設定を追加する。

- 同じMCP設定を重複させない。
- URLとクライアントバージョンは、このリポジトリの設定例を使う。
- トークン値は設定ファイルへ書かず、`WEB_DESIGN_PRO_TOKEN` を参照する。
- 編集後にTOML形式を確認する。

## 3. 契約者専用トークンを設定する

`WEB_DESIGN_PRO_TOKEN` が設定済みか、値を表示せず確認する。

未設定の場合は、利用者のOSとCodexの起動方法に合わせ、安全な入力方法を案内する。利用者がトークンを端末上で直接入力し、チャット・コマンド履歴・画面キャプチャへ残さないようにする。

AIがトークンの値を受け取る必要はない。入力後、値を表示せず設定の有無だけを確認する。

## 4. Codexを再起動する

再起動が必要な場合は、利用者へ次の文章を案内する。

> Codexを完全に終了して起動し直してください。その後「AIデザインSkillの初回設定の続きです。接続確認から再開してください」と送ってください。

再開後は完了済みの工程を繰り返さない。

## 5. AIデザインSkillの接続を確認する

利用できるMCP一覧または接続診断を使い、`web-design-pro` が接続済みであることを確認する。

利用可能な場合は `web_design_contract` を呼び、契約とクライアントバージョンが有効であることを確認する。認証拒否・契約停止・更新要求が返った場合は回避せず、秘密情報を除いた理由を利用者へ伝える。

## 6. Figmaを接続する

Figma連携が設定済みなら接続状態を確認する。未設定なら、現在のCodexで利用できるFigmaプラグインの導入と認証を案内する。

Figmaへのログイン、アカウント選択、アクセス許可は利用者本人が行う。対象Figmaが指定されている場合だけ、閲覧・編集権限を確認する。

## 7. 完了報告

次の項目を「確認済み／未完了／制作時に確認」に分けて報告する。

- AIデザインSkillの配置とCodexでの認識
- `web-design-pro` MCP接続
- 契約とクライアントバージョン
- Figma連携
- 対象Figmaへの権限（指定がある場合）

すべての必須項目が確認できたら、次の依頼例を案内する。

> AIデザインで、〇〇のTOPページを作りたいです。

## GitHubからの自動更新

AIデザインSkillは、制作開始時にGitHubの最新版を確認し、更新があればfast-forwardで反映する。利用者へ更新操作を求めない。

ローカル変更、GitHub以外の接続先、履歴の分岐を検出した場合は上書きしない。更新後は`SKILL.md`を読み直す。接続設定の変更がある場合だけ既存設定へ反映し、必要な場合のみCodexの再起動を案内する。
